# 2026-08-19

- **修复 FlyingBird 首启弹 PAL splash**：根因是 paf 默认不打包根 `<AppId>.ini`，全新安装后首启读不到 `DisableSplashScreen` → 因 `Launcher/splash.jpg` 存在而弹，首启后 Platform 写回根 ini 才不弹。最终方案是直接删 `splash.jpg`（PAL 逻辑：无图则强制 DisableSplashScreen=true）。对 FlyingBird/DirectoryOpus/XShellPlus 三款删除并提交（commit 6201852），重建 FlyingBird(00:19)/DOpus(00:20) paf 验证不含 splash.jpg。早前在 launcher 源加的 `ShowSplashScreen=false` 是无效键（PAL 不识别）。
- **环境异常**：操作 Apps/ 时反复遇 `Apps/` 整树被删空(129文件) + `index.lock` 持续重建，疑与沙箱/IDE 后台 git 轮询有关。已用 `rm -f .git/index.lock` + `rm`+指定 `git add` 绕过，数据经 `git checkout HEAD -- Apps/` 无损恢复。详见 MEMORY.md。
- 注意：`.workbuddy/memory/*` 仍按约定不提交（git status 显示为已跟踪的 ` M`，保留未暂存）。
- **FlyingBird 升级 3.1.8 + 修复坏 paf**：上一轮重建出的 `FlyingBirdPortable_3.0.3.paf.exe` 体积小(516KB)是因为 `AppFile` 残缺——早前 `Apps/` 被删后 `git checkout` 恢复时，被 gitignore 的 AppFile 没回来，只剩 api-ms 桩 dll，主程序(FlyingBird.exe/FlyingBirdCore.exe/flutter_windows.dll/dartjni.dll)全失。从 `D:\Scoop\apps\flyingbird\current`(3.1.8) 整体拷回 `App/AppFile/`(156MB,主程序齐全)，改 appinfo.ini 版本号，重建出 `FlyingBirdPortable_3.1.8.paf.exe`(41.7MB,含主程序、不含 splash.jpg)。删掉坏掉的旧 3.0.3 paf，提交版本号变更(commit ce9c5e6)。已将 Scoop 恢复源记进 MEMORY.md。
