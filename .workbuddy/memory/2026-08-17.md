# 2026-08-17

## PortableApps 项目分析 + 改进
- 项目：`D:\Code\xkyii\PortableApps`，个人 PA.c 便携软件仓库（9 个 app）。仓库只存配置/元数据，二进制 `App/AppFile` 被 .gitignore 忽略。
- 排定的 5 项改进（按序）：① 同步 README ② 重构 install.py 去硬编码+命令行 ③ build_all.py ④ installer.ini 下载源 ⑤ RapidEE launcher 定制。
- 已完成 ①：README 补 Thunder(12.3.0.3340)、XYplorer(27.00.600) 并修正命令行示例旧路径 `E:\xk\...` → `D:\Code\xkyii\...`。
- 已完成 ②：重写 Script/install.py，去硬编码（路径改走环境变量 PORTABLEAPPS_APPS_DIR / PORTABLEAPPS_PAF_DIR，APPS_DIR 默认从脚本位置推导）；argparse 支持 `app名`/`--list`/`--all`/多 app；多启动器数改为自动探测 appinfo{i}.ini（XShellPlus=x2）；缺工具时优雅报错退出。
