; FlyingBird Portable - installer.ini
; All user data is redirected to the portable Data directory via
; [DirectoriesMove] in the launcher, so there is nothing inside the App
; directory that must be preserved on upgrade. Kept minimal on purpose.
