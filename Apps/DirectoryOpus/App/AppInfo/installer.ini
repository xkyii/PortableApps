; Directory Opus Portable - installer.ini
; Local packaging mode: the App directory already contains all program files.
; No download-style install is used (personal use; binaries placed locally, not in git).

[Main]
InstallType=0
